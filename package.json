{
	"name": "rate-limit",
	"version": "1.0.0",
	"description": "A sample node app with rate limit implementation",
	"license": "MIT",
	"author": "Sumit Sahoo",
	"type": "module",
	"main": "app.js",
	"scripts": {
		"start": "pm2 start app.js --name rate-limit-app",
		"dev": "nodemon app.js",
		"format": "npx biome format ./ --write",
		"lint": "npx biome lint ./ --apply",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"dependencies": {
		"express": "^5.1.0",
		"express-rate-limit": "^7.5.0",
		"express-slow-down": "^2.1.0",
		"pm2": "^6.0.6"
	},
	"devDependencies": {
		"@biomejs/biome": "1.9.4",
		"nodemon": "^3.1.10"
	}
}
